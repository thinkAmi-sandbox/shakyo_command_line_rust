[package]
name = "catr"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = "2"

[dev-dependencies]
assert_cmd = "2"
predicates = "2"
rand = "0.8"

# テストコードの中で setup したいため、ctorを追加
# https://crates.io/crates/ctor
ctor = "0.2"